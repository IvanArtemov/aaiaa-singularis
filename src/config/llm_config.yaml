# Minimal LLM configuration for Nebius-only setup
active_provider: nebius

nebius:
  api_key_env: NEBIUS_API_KEY
  base_url: https://api.studio.nebius.ai/v1/
  models:
    chat: openai/gpt-oss-120b
    embeddings: BAAI/bge-en-icl

general:
  log_requests: true
