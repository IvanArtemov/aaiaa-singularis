# 🚀 Quick Start - Telegram Bot

Запуск Telegram бота для анализа научных статей за 5 минут!

## Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

## Шаг 2: Создание Telegram бота

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/botfather)
3. Отправьте команду `/newbot`
4. Введите имя бота (например: `My Knowledge Graph Bot`)
5. Введите username бота (например: `my_kg_bot`)
6. Скопируйте токен (формат: `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

## Шаг 3: Настройка окружения

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте:

```bash
# Telegram Bot
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather

# OpenAI
OPENAI_API_KEY=ваш_openai_ключ

# Опционально (можно оставить по умолчанию)
MAX_PDF_SIZE_MB=10
MAX_REQUESTS_PER_USER_PER_HOUR=5
```

## Шаг 4: Запуск бота

```bash
python scripts/run_telegram_bot.py
```

Вы увидите:
```
==================================================
TELEGRAM BOT - PDF to Knowledge Graph
==================================================

🤖 Starting Telegram Bot...
🚀 Bot is starting...
   Press Ctrl+C to stop
==================================================

Bot started successfully!
```

## Шаг 5: Использование

1. Найдите вашего бота в Telegram по username
2. Отправьте `/start`
3. Отправьте PDF научной статьи
4. Подождите 30-60 секунд
5. Получите SVG граф знаний!

## 📊 Примерный workflow

```
Пользователь → /start
Бот → Приветственное сообщение с инструкциями

Пользователь → [отправляет PDF]
Бот → "📥 Downloading PDF..."
Бот → "📄 Parsing PDF... ✅"
Бот → "🤖 Extracting knowledge... (30-60s)"
Бот → [отправляет SVG файл с графом]
Бот → Статистика: сущности, связи, время, стоимость
```

## ⚙️ Настройки (опционально)

В `.env` можно изменить:

```bash
# Максимальный размер PDF в мегабайтах
MAX_PDF_SIZE_MB=10

# Сколько запросов может сделать один пользователь в час
MAX_REQUESTS_PER_USER_PER_HOUR=5

# Максимальное время обработки (секунды)
PROCESSING_TIMEOUT_SECONDS=180

# Модель LLM для извлечения
LLM_MODEL=gpt-4o-mini
```

## 🐛 Troubleshooting

### Бот не отвечает
✅ Проверьте, что бот запущен (терминал должен показывать "Bot started successfully!")
✅ Проверьте `TELEGRAM_BOT_TOKEN` в `.env`
✅ Убедитесь, что нет опечаток в токене

### "API key not found"
✅ Проверьте `OPENAI_API_KEY` в `.env`
✅ Убедитесь, что файл `.env` находится в корне проекта

### PDF не обрабатывается
✅ Проверьте размер файла (максимум 10 MB по умолчанию)
✅ Убедитесь, что файл - валидный PDF
✅ Посмотрите в логи терминала на ошибки

### "Rate limit exceeded"
✅ Подождите 1 час с последнего запроса
✅ Или увеличьте `MAX_REQUESTS_PER_USER_PER_HOUR` в `.env`

## 📚 Дополнительно

- **Полная документация:** [`bot/README.md`](bot/README.md)
- **Конфигурация:** [`bot/config.py`](bot/config.py)
- **Примеры использования:** Отправьте `/help` боту

## 💡 Советы

1. **Тестирование:** Начните с небольшой статьи (2-5 страниц)
2. **Стоимость:** Каждая статья стоит ~$0.02-0.03
3. **Время:** Обработка занимает 30-60 секунд
4. **Формат:** Только PDF файлы поддерживаются

## 🎯 Что дальше?

После успешного запуска бота:

1. ✅ Протестируйте на нескольких статьях
2. ✅ Проверьте качество извлеченных сущностей
3. ✅ Настройте rate limiting под ваши нужды
4. ✅ Деплой на сервер для постоянной работы (см. `bot/README.md`)

---

**Вопросы?** Создайте issue в репозитории или проверьте [`bot/README.md`](bot/README.md)
